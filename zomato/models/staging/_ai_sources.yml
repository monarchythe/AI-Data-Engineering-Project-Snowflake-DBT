version: 2

sources:
  - name: ai
    database:  ZOMATO
    schema: AI
    description: "LLM-enriched tables produced by ai/enrich_reviews.py (OpenAI)."
    tables:
    - name: review_enriched
      description: "Per-review sentiment + topic + kkey issue from the LLM."

  